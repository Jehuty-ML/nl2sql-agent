# lumen-query-agent

LumenLearn（流明学堂）自然语言数据分析智能体 · **求职 / 开源 Demo**。

- 单 Agent ReAct 闭环（工具调用 → 证据落盘 → 交付）
- **仅 ClickHouse**（`events` + `users`）
- 固定分析：DAU、留存、学习漏斗、概览、渠道完课
- FastAPI + Vue 对话页 + 任务进度
- 合成数据声明：与 `analysis` 仓共用事件字典

> 本项目为全新实现，业务叙事为虚构开源学习社区。数据为 Synthetic Demo Only。

---

## 为什么固定分析必须用 slash（而不是关键词）

本仓库的路由刻意对齐私有参考项目 `内部参考实现` 的做法：

| 输入 | 执行路径 | 是否调用 LLM |
|------|----------|--------------|
| `/dau` `/funnel` `/retention` `/overview` `/channel` `/help` | `fixed_slash` | **否**，直接跑注册好的 CK SQL |
| 自然语言（哪怕含「日活 / 留存 / 漏斗」） | `agent_loop` | 有 Key 则 ReAct；无 Key 则提示改用 slash |

### 不要用中文关键词抢跑固定分析

早期 Demo 曾用「句子里出现日活/留存/完课 → 直接跑固定 SQL」。这和 `内部参考实现` 不一致，且容易**误伤**：

| 用户原意 | 关键词误伤示例 |
|----------|----------------|
| 「按渠道看完课」 | 「完课」命中漏斗，跑成学习漏斗而非渠道完课 |
| 「对比一下留存和日活」 | 只命中其中一个，另一半被吞掉 |
| 「漏斗里日活用户掉到哪」 | 「日活」抢跑 DAU，丢掉漏斗语境 |

`内部参考实现` 的规则是：

1. **只有显式 slash**（如 `/today_dashboard`）在进入 Agent 循环之前硬拦截，走固定看板，**不经过大模型**。
2. **自然语言一律进 Agent**；若问题确实对应标准指标，由模型调用 `get_fixed_analysis` 工具，而不是靠字符串 `in` 猜。

因此本仓库：

- 前端快捷按钮发送的是 `/dau`、`/retention` 等指令，而不是「日活」「留存」中文短句。
- `app/core/routing/slash_router.py` 只认以 `/` 开头的指令；禁止用关键词表做请求级抢跑。
- `FIXED_QUERIES` 里的 `keywords` 字段仅作元数据/测试参考，**不得**用于 `route_input`。

这样固定分析可稳定演示（不依赖 LLM、也不误路由），自然语言能力仍留给真正的 Agent 路径。

---

## 快速开始

```powershell
cd F:\data_analysis_agent\lumen-query-agent
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
copy .env.example .env

# 需先起 analysis 的 ClickHouse 并导入数据
# cd ..\analysis; docker compose up -d
# python .\scripts\generate_demo_data.py --seed 42 --to-clickhouse --truncate

uvicorn app.server.app:app --host 0.0.0.0 --port 6010 --reload
```

打开：`http://127.0.0.1:6010/`

## 前端（Vue）

```powershell
cd webui
npm install
npm run dev # http://127.0.0.1:5173 （代理到 6010）
# 或打包后由后端托管：
npm run build
```

视觉为 **Fraunces + Sora / 橡木色 + 琥珀点缀**，有意区别于公司项目青蓝 Tailwind 风。

## 配置

使用本地 `.env`（非 ）：见 `.env.example`。

LLM 通过 `LLM_PROVIDER` 切换：

| Provider | 说明 | 主要环境变量 |
|----------|------|----------------|
| `dashscope` | 通义 / 百炼（默认） | `DASHSCOPE_API_KEY` |
| `deepseek` | DeepSeek | `DEEPSEEK_API_KEY` |
| `ark` | 火山豆包 | `ARK_API_KEY` |
| `ollama` | 本地 Ollama | `OLLAMA_BASE_URL` |
| `openai` | OpenAI 官方 | `OPENAI_API_KEY` |

## API

- 健康检查：`GET /health`
- 对话：`POST /api/v1/chat`
- 任务进度：`GET /api/v1/task/{task_id}`

## 关联仓库

- 采集链路示例：`analysis`（分支 `demo/lumenlearn-opensource`）
- 事件契约：`app/bi/events_dictionary.json`

## 协议

MIT（全新项目，无 GPL 衍生代码）。
